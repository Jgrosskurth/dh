{
  "name": "drunken-hitmen-aem",
  "private": true,
  "description": "Drunken Hitmen Softball - AEM Edge Delivery Services Site",
  "version": "1.0.0",
  "scripts": {
    "lint:js": "eslint .",
    "lint:css": "stylelint blocks/**/*.css styles/*.css",
    "lint": "npm run lint:js && npm run lint:css"
  },
  "devDependencies": {
    "@adobe/aem-cli": "latest",
    "eslint": "^8.57.0",
    "stylelint": "^16.2.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
